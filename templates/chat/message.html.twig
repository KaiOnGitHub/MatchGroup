<div class="message">
    <div class="message-sender colorId-{{ message.randomColor }}">~ {{ message.sender }}</div>
    <div class="message-content">{{ message.message }}</div>
    <div class="message-date">{{ message.createdAt|date }}</div>
</div>
